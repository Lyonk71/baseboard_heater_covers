Get initial_length 

-----------------------------------------------------
if corner:
    subtract width of top board from measured length
    add width of top board to one endpiece
elif no_corner:
    pass
return adjusted_length
-----------------------------------------------------

adjusted_length = adjusted_length - Measurements.mid_feet * 2

topshelf_midpiece = round(adjusted_length/36)

#_of_middle_sections = round(measured_length/topshelf_midpiece)
